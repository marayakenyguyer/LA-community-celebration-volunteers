**Tag** — selectable filter chip for role categories and shift windows; pairs with the roles browser.

```jsx
<Tag selected onClick={toggle}>Wayfinding</Tag>
<Tag onRemove={() => remove(id)}>San Diego</Tag>
```
