**Input** — labeled text field for the volunteer application and any form. Civic-blue focus ring; supports leading icon, hint and error states.

```jsx
<Input label="Email address" type="email" placeholder="you@email.com"
       hint="We'll only use this for shift updates." />
<Input label="ZIP code" error="Enter a valid LA-area ZIP." />
```
