**Switch** — pill toggle for preferences (notifications, availability filters). `onChange` receives the new boolean.

```jsx
<Switch label="Text me about open shifts" checked={sms} onChange={setSms} accent="gold" />
```
