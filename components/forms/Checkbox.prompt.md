**Checkbox** — bold-outlined square for consent, availability and multi-select lists in the application flow.

```jsx
<Checkbox label="I'm available during the Paralympic Games period" checked={ok} onChange={e => setOk(e.target.checked)} />
```
